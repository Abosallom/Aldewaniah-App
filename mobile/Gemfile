source "https://rubygems.org"

# Pins Fastlane for the iOS TestFlight pipeline (see mobile/fastlane/).
# Gemfile.lock is generated by the first CI run (`bundle install` via
# ruby/setup-ruby's bundler-cache) and should be committed after that run
# so subsequent builds are reproducible.
gem "fastlane"
