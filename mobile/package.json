{
  "name": "aldewaniah-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper for the Aldewaniah PWA (iOS / Android).",
  "scripts": {
    "sync-web": "bash sync-web.sh",
    "ios": "npm run sync-web && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.1.2",
    "@capacitor/ios": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
